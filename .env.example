# ================================================================
# .env.example — Template for RBAC Service + PostgreSQL config
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# ⚠  NEVER commit .env to version control.
# ================================================================

# ── Django ───────────────────────────────────────────────────────
DJANGO_SETTINGS_MODULE=rbac_project.settings
SECRET_KEY=change-me-to-a-long-random-string

# ── Database ─────────────────────────────────────────────────────
DB_NAME=rbac_project
DB_USER=postgres
DB_PASSWORD=change-me
DB_PORT=5432
# DB_HOST is forced to 'db' (compose service name) inside the compose files

# ── Port mapping (host-side) ─────────────────────────────────────
# Postgres exposed on 5433 to avoid conflicts with a local install
DB_EXPOSED_PORT=5433
RBAC_PORT=8002

# ── Image tag (org deployment only) ──────────────────────────────
# Set this to the tag you pushed with scripts/build-org.sh
RBAC_IMAGE_TAG=latest

# ── Organisation registry (org deployment only) ──────────────────
# Override if your private registry address changes
ORG_REGISTRY=192.168.71.244:30444
ORG_REGISTRY_USER=Hiren
